## Working on redux-saga example

1. Create react app using the command npx create-react-app <application_name>
2. Navigate to project folder and install the redux-saga module using the npm i redux-saga.
